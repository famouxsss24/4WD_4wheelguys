# --- Core Execution Dependencies (For Raspberry Pi / Autonomous Vehicle) ---
numpy
opencv-python
ai-edge-litert
ultralytics
gpiozero
Pillow

# Note: picamera2 is required for camera module v3.
# On Raspberry Pi OS, it is recommended to install picamera2 via system package manager:
# sudo apt update && sudo apt install python3-picamera2
# Or via pip (if system-level libcamera is configured):
# picamera2

# --- Model Training & Development Dependencies (Optional, for Jupyter Notebooks) ---
# tensorflow
# pandas
# scikit-learn
# imgaug
# matplotlib
# scipy
